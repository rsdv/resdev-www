/**
 * Hold information regarding the device the user is using
 *
 * Just helps with map views
 * */
import { createContext } from 'react'

const DeviceContext = createContext({})

export default DeviceContext
